---
title: 樣式測試
author: 學生議員潘祈瑋
pubDatetime: 2025-11-08T03:42:01Z
modDatetime: 
slug: style-test
featured: false
draft: true
tags:
  - 測試
description: 樣式測試：寫一點介紹，測試各種Markdown與特殊樣式。
---

[SA-Law]: https://ashssa.github.io/laws/ "高師大附中學生會自治法規共用系統"

> 這是一個標準的段落，用於測試基本的文字顯示、行距以及字體設定。

<!-- ## Table of contents -->
<!-- Table of contents placeholder, usually generated automatically by the engine -->

## 標題與分界線測試 (H2)

### 三級標題 (H3)

#### 四級標題 (H4)

##### 五級標題 (H5)

###### 六級標題 (H6)

---

```markdown
## 標題與分界線測試 (H2)

### 三級標題 (H3)

#### 四級標題 (H4)

##### 五級標題 (H5)

###### 六級標題 (H6)

---
```

## 基本文字樣式

這是**粗體文字**，這是*斜體文字*，這是**_粗斜體文字_**。

這是~~刪除線文字~~。

這是**粗體**與*斜體*的混排。

這是包含[內部連結][SA-Law]的文字，其中這是[重複使用的連結][SA-Law]。

```markdown
這是**粗體文字**，這是*斜體文字*，這是**_粗斜體文字_**。

這是~~刪除線文字~~。

這是**粗體**與*斜體*的混排。

[SA-Law]: https://ashssa.github.io/laws/ "高師大附中學生會自治法規共用系統"

這是包含[內部連結][SA-Law]的文字，其中這是[重複使用的連結][SA-Law]。
```


## 列表測試

### 無序列表
 * 項目一：這是一個無序列表。
 * 項目二：
   * 次項目 2.1
   * 次項目 2.2
 * 項目三：這是一個較長的列表項目，用於測試換行與縮排。不夠長不夠長不夠長不夠長不夠長不夠長不夠長不夠長不夠長不夠長不夠長不夠長不夠長不夠長不夠長。

```markdown
 * 項目一：這是一個無序列表。
 * 項目二：
   * 次項目 2.1
   * 次項目 2.2
 * 項目三：這是一個較長的列表項目，用於測試換行與縮排。不夠長不夠長不夠長不夠長不夠長不夠長不夠長不夠長不夠長不夠長不夠長不夠長不夠長不夠長不夠長。
```

### 有序列表
 1. 第一項
 1. 第二項
   1. 次項 A
   1. 次項 B
 1. 第三項

```markdown
 1. 第一項
 1. 第二項
   1. 次項 A
   1. 次項 B
 1. 第三項
```

## 程式碼區塊

### 行內程式碼

你可以使用`行內程式碼`來顯示短小的程式碼片段或指令。

### 圍欄式程式碼區塊

這裡是一個程式碼區塊的測試，使用 Shiki 高亮。
```javascript
function calculateSum(a, b) {
  // 這是一個註解
  const result = a + b;
  if (result > 10) {
    console.log("結果大於 10");
  }
  return result;
}

calculateSum(5, 7);
```

## 表格測試

| 標題 A | 標題 B | 標題 C |
|---|---|---|
| 左對齊 | 置中對齊 | 右對齊 |
| 123 | 456 | 789 |
| 較長的內容測試 | 內容 | 另一個內容 |

> 經測試，[Astro Paper](https://github.com/satnaing/astro-paper)的表格，是沒有響應式設計的。

```markdown
| 標題 A | 標題 B | 標題 C |
|---|---|---|
| 左對齊 | 置中對齊 | 右對齊 |
| 123 | 456 | 789 |
| 較長的內容測試 | 內容 | 另一個內容 |
```

## 圖片與引言

### 圖片測試

![高師大附中學生會自治法規共用系統宣傳圖](@/assets/images/SALaw-1.png "高師大附中學生會自治法規共用系統宣傳圖")

```markdown
![高師大附中學生會自治法規共用系統宣傳圖](@/assets/images/SALaw-1.png "高師大附中學生會自治法規共用系統宣傳圖")
```

### 巢狀引言

> 這是一級引言區塊。
>
> 這是第二行。
> > 這是巢狀的二級引言。
> > 
> > > 這是三級引言。
> > > 
> > 
> 

```markdown
> 這是一級引言區塊。
>
> 這是第二行。
> > 這是巢狀的二級引言。
> > 
> > > 這是三級引言。
> > > 
> > 
> 
```

## 數學符號（統一協定）

我們可以在行內測試數學方程式： \alpha^2 + \beta^2 = \gamma^2 。

也可以在獨立的區塊測試顯示方程式：

\int_{-\infty}^{\infty} e^{-x^2} dx = \sqrt{\pi} 

```markdown
我們可以在行內測試數學方程式： \alpha^2 + \beta^2 = \gamma^2 。

也可以在獨立的區塊測試顯示方程式：

\int_{-\infty}^{\infty} e^{-x^2} dx = \sqrt{\pi} 

```

## 數學符號（統一協定）

我們可以在行內測試數學方程式： \alpha^2 + \beta^2 = \gamma^2 。

也可以在獨立的區塊測試顯示方程式：

\int_{-\infty}^{\infty} e^{-x^2} dx = \sqrt{\pi} 

```markdown
我們可以在行內測試數學方程式： \alpha^2 + \beta^2 = \gamma^2 。

也可以在獨立的區塊測試顯示方程式：

\int_{-\infty}^{\infty} e^{-x^2} dx = \sqrt{\pi} 

```

> Astro Paper其實是可以正常插入LaTeX公式的，請見[這個Blog](https://astro-paper.pages.dev/posts/how-to-add-latex-equations-in-blog-posts/)。


## 特殊符號與轉義
 * 星號 * 不會變成列表項目。
 * 井字號 # 不會變成標題。
 * 反引號 ` 不會變成行內程式碼。

## 進階樣式

要被突顯的重點，就用==「雙等號」==括起來！這是一個特殊的高亮樣式，應該會被黃色背景突顯。

這是一句需要被註解的話[^1]。

[^1]: 註解內容。

```markdown
要被突顯的重點，就用==「雙等號」==括起來！這是一個特殊的高亮樣式，應該會被黃色背景突顯。

這是一句需要被註解的話[^1]。

[^1]: 註解內容。
```

本文章的最後一行內容在此。